- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles:    ''
    ~/.zsh:         zsh
    ~/.zshrc:       zshrc
    ~/.zprofile:    zprofile

- shell:
  - [git submodule update --init --recursive, Installing submodules]
